[package]
name = "ordered-json"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
description = "Strict JSON with insertion-ordered associative objects and exact number literals"
readme = "README.md"
repository = "https://github.com/polyspec/ordered-json.git"

[lib]
path = "src/lib.rs"

[dev-dependencies]
sha2 = "0.10"
serde_json = "1"
